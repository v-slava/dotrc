# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $DOTRC DOTRC_TEMPLATE
set $DOTRC_S DOTRC_S_TEMPLATE
set $OUTPUT_TRAY OUTPUT_TRAY_TEMPLATE
set $START DOTRC_TEMPLATE/other_files/start_from_gui.sh
set $START_S DOTRC_TEMPLATE/other_files/start_singleton_gui_program.py
set $WWW_START_PAGE $DOTRC/other_files/start_page.html

set $OUTPUT_1 OUTPUT_1_TEMPLATE
set $OUTPUT_2 OUTPUT_2_TEMPLATE
set $OUTPUT_3 OUTPUT_3_TEMPLATE
set $OUTPUT_4 OUTPUT_4_TEMPLATE
set $OUTPUT_5 OUTPUT_5_TEMPLATE
set $OUTPUT_6 OUTPUT_6_TEMPLATE
set $OUTPUT_7 OUTPUT_7_TEMPLATE
set $OUTPUT_8 OUTPUT_8_TEMPLATE
set $OUTPUT_9 OUTPUT_9_TEMPLATE

# standard keyboard modifiers according to xmodmap(1)
# see xmodmap {-pk|-pke} for complete keymap table
set $Alt_L Mod1
set $Alt_R Mod3
set $WIN Mod4
set $Ctrl_L Control
set $Ctrl_R Control
set $Shift_L Shift
set $Shift_R Shift
set $PrintScreen 107
# set $Menu 135

bindsym $Alt_R+e exec --no-startup-id exec $START $DOTRC_S/i3_hotkey_handler.sh

# font for window title
font pango:Inconsolata LGC 8

# focus_on_window_activation focus

# use Mouse+$Alt_L to drag floating windows to their wanted position
floating_modifier $Alt_L

# kill focused window
bindsym $Alt_R+$Shift_R+Q kill
bindsym $WIN+Escape kill

# "minimize" window (move to workspace 0):
bindsym $WIN+Tab move container to workspace 0

# mark window with label for further fast access:
bindsym $Alt_L+m exec --no-startup-id exec $START $DOTRC/other_files/i3_mark_window.sh
# goto window by mark (label name):
bindcode $Alt_L+48 exec --no-startup-id exec $START $DOTRC/other_files/i3_goto_marked_window.py

# switch to virtual console 2
bindsym $Ctrl_L+$ALT_L+F2 exec --no-startup-id exec sudo chvt 2

# change focus
bindsym $Alt_L+h focus left
bindsym $Alt_L+j focus down
bindsym $Alt_L+k focus up
bindsym $Alt_L+l focus right

# move focused window
bindsym $Alt_L+$Shift_L+h move left
bindsym $Alt_L+$Shift_L+j move down
bindsym $Alt_L+$Shift_L+k move up
bindsym $Alt_L+$Shift_L+l move right

# split in horizontal orientation
bindsym $Alt_R+h split h

# split in vertical orientation
bindsym $Alt_R+v split v

# enter fullscreen mode for the focused container
bindsym $Alt_L+f fullscreen

# change container layout (stacked, tabbed, default)
# bindsym $Alt_L+s layout stacking
bindsym $Alt_L+w layout tabbed
bindsym $Alt_L+e layout default

# toggle tiling / floating
bindsym $Alt_L+$Shift_L+space floating toggle

# change focus between tiling / floating windows
bindsym $Alt_L+space focus mode_toggle

# focus the parent container (father)
bindsym $Alt_R+f focus parent

# focus the child container
bindsym $Alt_R+C focus child

# switch to workspace
bindsym $Alt_L+0 workspace 0
bindsym $Alt_L+1 workspace 1
# bindsym $Alt_L+1 workspace 1: main
bindsym $Alt_L+2 workspace 2
bindsym $Alt_L+3 workspace 3
bindsym $Alt_L+4 workspace 4
bindsym $Alt_L+5 workspace 5
bindsym $Alt_L+6 workspace 6
bindsym $Alt_L+7 workspace 7
bindsym $Alt_L+8 workspace 8
bindsym $Alt_L+9 workspace 9

# exec --no-startup-id sleep 4 && $DOTRC/other_files/xrandr.sh xinitrc && exec sudo udevadm trigger --subsystem-match=input --action=change

workspace m output $OUTPUT_TRAY

workspace 1 output $OUTPUT_1
workspace 2 output $OUTPUT_2
workspace 3 output $OUTPUT_3
workspace 4 output $OUTPUT_4
workspace 5 output $OUTPUT_5
workspace 6 output $OUTPUT_6
workspace 7 output $OUTPUT_7
workspace 8 output $OUTPUT_8
workspace 9 output $OUTPUT_9

# Start i3bar:
bar {
	# status_command exec i3status
	status_command i3status | $DOTRC/other_files/i3_status_wrapper.py
	tray_output $OUTPUT_TRAY
}

# Focus initial workspace:
exec --no-startup-id exec i3-msg "workspace 1"

# move focused container to workspace
bindsym $Alt_L+$Shift_L+parenright move container to workspace 0
bindsym $Alt_R+$Shift_R+exclam move container to workspace 1
bindsym $Alt_R+$Shift_R+at move container to workspace 2
bindsym $Alt_R+$Shift_R+numbersign move container to workspace 3
bindsym $Alt_R+$Shift_R+dollar move container to workspace 4
bindsym $Alt_R+$Shift_R+percent move container to workspace 5
bindsym $Alt_R+$Shift_R+asciicircum move container to workspace 6
bindsym $Alt_L+$Shift_L+asciicircum move container to workspace 6
bindsym $Alt_L+$Shift_L+ampersand move container to workspace 7
bindsym $Alt_L+$Shift_L+asterisk move container to workspace 8
bindsym $Alt_L+$Shift_L+parenleft move container to workspace 9

# reload the configuration file
bindsym $Alt_R+$Shift_R+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $Alt_R+$Shift_R+R restart
# exit i3 (logs you out of your X session)
bindsym $Alt_R+$Shift_R+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
	# These bindings trigger as soon as you enter the resize mode
	bindsym h resize shrink width 10 px or 1 ppt
	bindsym j resize grow height 10 px or 1 ppt
	bindsym k resize shrink height 10 px or 1 ppt
	bindsym l resize grow width 10 px or 1 ppt

	# back to normal: Enter or Escape
	bindsym Return mode "default"
	bindsym Escape mode "default"
}

bindsym $Alt_L+r mode "resize"

for_window [workspace="0"] layout tabbed
for_window [title="Choose.*by pressing"] move container to workspace 0
for_window [title="background_window"] move container to workspace 0
for_window [title="FVP terminal_*"] move container to workspace 0
for_window [class="kolourpaint"] move container to workspace 0
for_window [window_role="browser"] mark --replace browser, move container to workspace m
# Set floating mode for some programs:
for_window [title="Qemu: Windows 7"] fullscreen
for_window [title="My bluetooth headset"] floating enable
# for_window [class="Skype"] floating enable
for_window [class="Skype"] mark --replace skype
for_window [class="TelegramDesktop"] mark --replace telegram
for_window [class="Gimp"] floating enable
for_window [class="GoldenDict"] floating disable
for_window [class="Claws-mail" title="Search messages"] floating enable
for_window [class="Claws-mail" title="Find in current message"] floating enable
for_window [class="Icedove" title="Search Messages"] floating enable
for_window [class="Icedove"] focus, mark --replace mail
for_window [class="Thunderbird" title="Search Messages"] floating enable
for_window [class="Thunderbird"] focus, mark --replace mail, move container to workspace m
for_window [class="ViberPC"] mark --replace viber
for_window [class="TeamViewer" title="TeamViewer"] mark --replace teamviewer
for_window [class="TeamViewer" title="Computers & Contacts"] kill
# Login to some stupid GUI program like team viewer:
# bindsym $Alt_R+i exec --no-startup-id sleep 1 && exec xdotool type "your_password"

bindsym $Alt_R+l exec --no-startup-id exec $START $DOTRC/other_files/lock_screen.sh i3_hotkey
bindsym $Ctrl_L+$Alt_L+r exec --no-startup-id exec sudo reboot
bindsym $Ctrl_L+$Alt_L+s exec --no-startup-id exec $START $DOTRC/other_files/suspend_hotkey.sh
# bindsym $Ctrl_L+$Alt_L+h exec sudo poweroff
bindsym $Ctrl_l+$Alt_L+h exec --no-startup-id exec $START $DOTRC/other_files/hibernate_hotkey.sh
bindsym $Ctrl_l+$WIN+h exec --no-startup-id exec $START $DOTRC/other_files/hibernate_hotkey.sh --sleep 180
for_window [class="URxvt" title="hibernate script"] floating enable
for_window [class="URxvt" title="suspend script"] floating enable

bindsym $WIN+k exec --no-startup-id exec keynav

# bindsym $Alt_L+Return exec exec x-terminal-emulator -e vifm
# bindsym $Alt_L+Return exec exec $DOTRC/other_files/open_terminal.sh vifm
bindsym $Alt_L+Return exec exec $START $DOTRC/other_files/open_terminal.sh vifm ~/my ~/my

bindsym $Alt_L+t exec --no-startup-id exec $START_S dictionary "$(clipboard.sh -o)"
bindsym $Alt_L+q exec --no-startup-id $(clipboard.sh --primary-2-clipboard) && exec $START_S dictionary "$(clipboard.sh -o)"
# pronounce (say) word from clipboard:
bindsym $Alt_L+s exec --no-startup-id exec $START $DOTRC/other_files/say_word.sh "$(clipboard.sh -o)"
bindsym $WIN+c exec exec $START qalculate

bindsym $WIN+a exec --no-startup-id exec $START $DOTRC/other_files/start_anki.sh

# browser
bindsym XF86HomePage exec --no-startup-id exec $START_S browser $WWW_START_PAGE
bindsym $Alt_L+b exec --no-startup-id exec $START_S browser $WWW_START_PAGE

bindsym $Alt_R+$Shift_R+t exec --no-startup-id exec $START_S telegram
exec --no-startup-id exec $START telegram

# bindsym $Alt_R+$Shift_R+v exec --no-startup-id exec /opt/viber/Viber
# exec --no-startup-id exec /opt/viber/Viber
bindsym $Alt_R+$Shift_R+s exec --no-startup-id exec $START_S skype
# sometimes in skype I don't hear sound (see pulseaudio). Restart of skype
# helps in this case. Try starting skype with delay (hope it will help too):
exec --no-startup-id sleep 5 && $START_S skype

# windows guest
# bindsym $Ctrl_L+$Alt_L+q exec exec $START $DOTRC/other_files/qemu_start.sh

# autostart mail client (claws-mail):
# exec --no-startup-id exec i3-msg 'workspace 1; exec exec $START claws-mail'
# Hotkey to open mail client:
# bindsym $WIN+m exec --no-startup-id exec $START claws-mail
# bindsym $WIN+m exec --no-startup-id exec $START thunderbird
# Hotkeys to open specified folders:
# bindsym $Alt_R+$Shift_R+v exec --no-startup-id exec $START claws-mail --select '#imap/viacheslav.volkov.1/INBOX'
# bindsym $Alt_R+$Shift_R+s exec --no-startup-id exec $START claws-mail --select '#imap/slava65536/INBOX'

# Start emacs daemon:
# exec --no-startup-id exec emacs --daemon

# Start mail client (icedove/thunderbird):
exec --no-startup-id $DOTRC/other_files/virtual_box.sh || $DOTRC/other_files/mail_client.sh

# Switch to "mail" workspace:
bindsym $WIN+m workspace m

# Start network manager applet:
exec --no-startup-id exec $START nm-applet

# Start keyboard layout indicator/switcher:
exec --no-startup-id exec $START $DOTRC/other_files/init_keyboard_layout.sh

# Switch en <--> de keyboard layout:
bindsym $WIN+s exec --no-startup-id exec $START $DOTRC/other_files/switch_us_de_keyboard_layout.sh

# Delete system update lock directory:
exec --no-startup-id exec rm -rf /tmp/system_update_lock_dir

# audio player hotkeys:
bindsym $Alt_L+p      exec --no-startup-id exec $START $DOTRC/other_files/my_play_pause.sh
bindsym $Alt_R+p      exec --no-startup-id exec $START $DOTRC/other_files/my_play_pause.sh
bindsym XF86AudioPlay exec --no-startup-id exec $START $DOTRC/other_files/my_play_pause.sh

# alsaplayer:
# exec --no-startup-id exec alsaplayer -i daemon --looplist --startvolume 1.0 1>/dev/null &
# bindsym XF86AudioNext exec --no-startup-id exec alsaplayer --next
# bindsym $WIN+n        exec --no-startup-id exec alsaplayer --next
# bindsym XF86AudioPrev exec --no-startup-id exec alsaplayer --prev
# bindsym $WIN+p        exec --no-startup-id exec alsaplayer --prev
# bindsym $WIN+h        exec --no-startup-id exec alsaplayer --relative -3
# bindsym $WIN+l        exec --no-startup-id exec alsaplayer --relative +3
# bindsym $WIN+0        exec --no-startup-id exec alsaplayer --seek 0

# xmms2:
bindsym XF86AudioNext exec --no-startup-id exec $START xmms2 next
bindsym $WIN+n        exec --no-startup-id exec $START xmms2 next
bindsym XF86AudioPrev exec --no-startup-id exec $START xmms2 prev
bindsym $WIN+p        exec --no-startup-id exec $START xmms2 prev
bindsym $WIN+h        exec --no-startup-id exec $START xmms2 seek -3
bindsym $WIN+l        exec --no-startup-id exec $START xmms2 seek +3
bindsym $WIN+0        exec --no-startup-id exec $START xmms2 seek 0

# volume hotkeys:
bindsym XF86AudioMute        exec --no-startup-id exec $START $DOTRC/other_files/set_volume.sh 0%
bindsym XF86AudioRaiseVolume exec --no-startup-id exec $START $DOTRC/other_files/set_volume.sh +5%
bindsym $WIN+u exec --no-startup-id exec $START $DOTRC/other_files/set_volume.sh +5%
bindsym XF86AudioLowerVolume exec --no-startup-id exec $START $DOTRC/other_files/set_volume.sh -5%
bindsym $WIN+d exec --no-startup-id exec $START $DOTRC/other_files/set_volume.sh -5%

# toggle sound output (bluetooth headset / built-in audio):
bindsym $Alt_R+$Shift_R+b exec --no-startup-id exec $START $DOTRC/other_files/bluetooth_toggle_hotkey.sh
bindsym $Alt_L+$Shift_L+b exec --no-startup-id exec $START $DOTRC/other_files/bluetooth_toggle_hotkey.sh

# brightness hotkeys:
bindsym XF86MonBrightnessUp exec --no-startup-id exec $START $DOTRC/other_files/screen_brightness.sh --up
bindsym XF86MonBrightnessDown exec --no-startup-id exec $START $DOTRC/other_files/screen_brightness.sh --down
bindsym $Alt_r+u exec --no-startup-id exec $START $DOTRC/other_files/screen_brightness.sh --up
bindsym $Alt_r+d exec --no-startup-id exec $START $DOTRC/other_files/screen_brightness.sh --down

# toggle keyboard layout (see also $DOTRC/root_settings/etc/default/keyboard):
bindsym $Alt_L+F3 exec --no-startup-id exec $START /media/files/programs/keyboard_layout --set-layout next

# toggle touchpad:
bindsym $Alt_L+F2 exec --no-startup-id exec $START $DOTRC/other_files/toggle_touchpad.sh
bindsym XF86TouchpadToggle exec --no-startup-id exec $START $DOTRC/other_files/toggle_touchpad.sh

# turn off touchpad by default:
exec --no-startup-id exec $START $DOTRC/other_files/toggle_touchpad.sh

# dmenu (a program launcher)
bindsym $Alt_L+d exec --no-startup-id exec $START $DOTRC/other_files/dmenu.sh

# noob settings begin
# noob settings end

# exec --no-startup-id i3-msg 'workspace 2; exec uzbl -u URL'

bindsym $WIN+$Shift_L+s exec --no-startup-id exec $START $DOTRC/other_files/xrandr.sh --same-as
bindsym $WIN+$Shift_L+h exec --no-startup-id exec $START $DOTRC/other_files/xrandr.sh --left-of
bindsym $WIN+$Shift_L+l exec --no-startup-id exec $START $DOTRC/other_files/xrandr.sh --right-of

bindcode $PrintScreen exec --no-startup-id exec $START $DOTRC/other_files/screenshot.sh
bindcode $Alt_L+$PrintScreen exec --no-startup-id exec $START $DOTRC/other_files/screenshot.sh --focused --border
bindcode $WIN+$PrintScreen exec --no-startup-id $START $DOTRC/other_files/screenshot.sh --my-sleep 6 --my-edit-image
bindsym $WIN+q exec --no-startup-id exec $START $DOTRC/other_files/screenshot.sh --my-edit-image --focused --border

# CapsLock (caps lock):
bindsym $WIN+j exec --no-startup-id exec $START xdotool key Caps_Lock
