set font "Inconsolata LGC 14"
set selection-clipboard clipboard

# The following fixes opening https:// links in google-chrome (if firefox is not
# installed):
set sandbox none

unmap d

" set scroll-step 100
" set scroll-hstep 100

" unmap q
" unmap <Space>
" " map <Space>qm quit " doesn't work (bug?)
" map qm quit

" The following is not supported:
" set smooth-scroll true
" set smooth-scroll "true"
