#!/bin/bash

# Kill first process group.

# Usage:
# pl | grep record | pgk
# pl | grep record | grep wav | pgk -9

set -e

if [ $# -eq 0 ] ; then
	ARGS="-TERM"
else
	ARGS="$@"
fi

PGID=$(head -n 1 | awk '{print $1}')
kill "$ARGS" "-${PGID}"

